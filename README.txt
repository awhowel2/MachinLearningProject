The .py should run on their own but modules may be needed to be downloaded. If the ann visual won't run because
of the import then it can be commented out. Each code should take about 20 minutes to learn. The base directory 
has to be changed to the proper one to get the images. The picture data is not included because it makes the file 
too large to submit. 

Images can be found at: http://cegp.ece.gatech.edu/codedata/landmass/
